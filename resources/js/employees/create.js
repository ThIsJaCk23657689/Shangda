Vue.component('employee-form-page', require('./../components/Employees/EmployeeForm.vue').default);

const app = new Vue({
    el: '#employee-app',
    data() {
        return {
        }
    },
    methods: {
    },
    created() {
    },
    mounted() {
    }
});
